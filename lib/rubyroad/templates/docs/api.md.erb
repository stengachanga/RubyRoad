# <%= a.title %> — API reference

Generated by RubyRoad <%= Rubyroad::VERSION %> from OpenAPI `<%= a.version %>` (<%= a.operations.size %> operations).

Base URLs:

- Sandbox: `<%= a.sandbox_url %>`
- Live: `<%= a.live_url %>`

Every method below is implemented on `<%= module_name %>::Client` with a real Faraday request body — not an empty stub.

<%= codegen.docs_operations %>
